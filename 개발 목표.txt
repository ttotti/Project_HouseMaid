* ~ 지금까지의 목표
- UI 구현
- 폴더 및 파일 생성 구현
- 각 폴더마다 스택위젯 생성 후 파일위젯을 넣어서 폴더 선택 시 해당 파일 위젯이 노출되게 구현
- 파일 위젯에 각 파일마다 스택위젯을 생성 후 텍스트에디터를 넣어서 파일 선택 시 해당 텍스트에디터가 노출되게 구현
- 폴더 및 파일 삭제 시 각 리스트에서 삭제되며 새 폴더 및 새 파일이 정상적으로 생성되게 구현
- 파일 삭제 시 텍스트 에디터 위젯 삭제 구현(삭제 시 다른 파일의 텍스트 노출되게) -> 우선구현(완료)
- 폴더 삭제 시 텍스트 에디터 위젯 삭제 구현(완료)
- 다른 폴더 선택 시 해당 폴더의 마지막으로 선택한 파일의 텍스트에디터 노출 구현 -> 수정 필요(완료)
- 파일 이름 변경 구현(완료)

* 다음 목표
- 폴더 및 파일 삭제 후 리스트에서 정상적으로 사라지는지 확인
- 삭제 후 새 폴더 및 새 파일이 정상적으로 생성되는지 확인
- 텍스트에디터 contextmenu 구현(필요 시 구현)
- 텍스트 취소선, 굵게 등 편집기 구현
- 텍스트 하이퍼링크 구현 -> Ctrl 키 누르면 setReadOnly가 True로 바뀌면서 링크누를수있게 구현?
- 저장 및 불러오기 구현



* 2021-09-09
- 폴더 선택 시 해당 폴더의 첫번째 파일의 텍스트에디터가 노출되게 구현함
-> 목표에 맞게 수정 필요

- 폴더 삭제 시 텍스트 에디터 위젯 삭제 구현
-> 삭제 시 0번(더미데이터) 파일위젯 및 텍스트에디터 위젯 노출되게 구현


* 2021-09-10
- 파일 삭제 시 텍스트 에디터 위젯 삭제 구현(삭제 시 다른 파일의 텍스트 노출되게)
-> 삭제 시 해당 번호-1번 노출되게 구현(파일 2개 이상일때 0번 파일 삭제 시 그대로 0번 파일 노출되게 구현)]

- 기타 오류 수정
- 파일 수정 후 폴더 생성 시 해당 파일내용이 노출되는 현상
-> 폴더 생성 시 더미데이터로 넣은 0번 파일 및 0번 텍스트에디터 노출되게 구현

- 폴더 및 파일 삭제 후 선택되지 않았는데 삭제 버튼이 활성화로 노출되는 현상
-> 삭제 시 삭제버튼이 비 활성화로 노출되게 구현
-> 필요 시 삭제 시 다른 폴더 및 파일이 선택되게 수정


# 2021-09-13
- 다른 폴더 선택 시 해당 폴더의 마지막으로 선택한 파일의 텍스트에디터 노출 구현
-> 구현 완료

- 파일 이름 변경 구현
=> 구현 완료


# 2021-10-15
- pickle을 사용해서 저장 구현
-> 폴더별로 구분되서 저장되는데 폴더 안에서도 파일끼리 구분되게 구현
-> 파일삭제 시 filesave 해당 파일 리스트에서 제거 구현
